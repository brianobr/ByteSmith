// In-memory database implementation - no external database required
import * as schema from "@shared/schema";

// Mock database for in-memory storage
export const db = null; // Not used with in-memory storage
export const pool = null; // Not used with in-memory storage